# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This repository is in early initialization. Currently it contains only Claude Code configuration (`.claude/settings.json`) with the `superpowers@claude-plugins-official` plugin enabled.

Add build, test, and lint commands here once the project has a defined stack.
